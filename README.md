# OADP シナリオ分離型レースシミュレーター（試作版）

JRA出馬表テキストを入力し、OADPの「S1/S2/S3は着順違いではなく4角到達構造の違い」という原則に沿って、異なる入力世界を作り、同一のモンテカルロエンジンで走らせます。

## 起動
```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```

## 実装済み
- JRAプレーンテキストの基本パース
- FACTから基礎DERIVED/ESTIMATEを生成
- ハナ取得確率、先手参加率
- 前列圧発生/被害、序盤位置不発
- 差し接続、残存エネルギー、進路実現、失速率
- S1/S2/S3別モンテカルロ
- 4角順位平均/SD、勝率、複勝率
- シナリオ独立監査
- CSV/JSON出力

## 制限
OADPで係数が完全固定されていない部分は暫定式です。人気・オッズは物理計算とシナリオ発生確率に使用しません。


## v0.1.1 修正内容

- 馬ブロック抽出を空行・全角空白・「枠1 / 1枠」表記へ対応
- 抽出馬数が0頭の場合は「基礎数値を生成」ボタンを無効化
- 空データや必須列不足時の `KeyError: '馬番'` を防止
- 入力失敗時に古いセッション結果を誤使用しないよう改善
- 基礎数値生成・シミュレーション実行時の例外を画面上へ表示


## v0.1.2 修正内容

- `枠1白<TAB>1` のように枠・枠色・馬番が同じ行にあるJRAコピー形式へ対応
- `枠3赤` と次行の `3` のような分離形式にも対応
- ブリンカー行が馬名の前に入る形式へ対応
- 枠見出し行の誤認防止を追加
- 馬ブロック0頭時の実行停止を維持


## v0.1.3 修正内容

v0.1.2では、正規表現中の `\s`、`\d`、タブ・改行正規化文字列が
二重エスケープされ、実際の空白・数字・タブとして認識されない不具合がありました。

- 二重エスケープを全面修正
- Windows/Unix/Mac改行を `splitlines()` で統一処理
- タブ、全角空白、NBSP、BOM、ゼロ幅空白を正規化
- `枠1白<TAB>1` 形式の14頭抽出を実データ相当テストで確認
- ブリンカー行を飛ばして馬名を取得
- 画面にParser versionを表示


## v0.2.0 追加内容

- STEP0作成アプリ v3.8 の `parse_nar_html()` を統合
- 地方競馬公式 `keiba.go.jp` の出馬表URL入力に対応
- URL取得後、STEP0と同じ Race/Horse/PastRun 構造をシミュレーション共通入力へ変換
- 地方競馬URLとJRAテキストの入力経路を分離
- 取得頭数0件時は計算を停止
- 人気・オッズは取得しても物理計算へ使用しない

地方競馬URLの例:

`https://www.keiba.go.jp/KeibaWeb/TodayRaceInfo/DebaTable?k_raceDate=2026%2F07%2F26&k_raceNo=12&k_babaCode=10`


## v0.2.1 修正内容

- シミュレーション実行中の進捗表示を追加
- 完了後に `st.rerun()` して結果表示を確実化
- Session Stateをキー単位で保存
- 入力件数・結果件数の整合性検査を追加
- 失敗時にPythonトレースバックを画面表示
- 前回結果を消去してから再実行


## v0.3.0 OADP Ver.2.35反映

- 展開構造を馬評価より先に生成
- 人気・オッズをシナリオ発生確率・物理入力へ不使用
- S1/S2/S3で別々の4角到達式を使用
- S3を軽斤量・内枠・差し接続・反相関複合で生成
- OADPシナリオ専用EV、構造穴EVをシミュレーション入力へ追加
- 2026年7月26日 高知9RでAI/OADP計算例を同梱
